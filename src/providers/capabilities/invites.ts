// Guild and channel invites — populated by PR 4 (feat/invites-dms).

export interface InviteCapability {
    // Methods added by PR 4: list_invites, list_channel_invites, get_invite,
    // create_invite, delete_invite
}
