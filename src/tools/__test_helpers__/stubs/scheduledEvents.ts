import type { ScheduledEventCapability } from '../../../providers/capabilities/scheduledEvents.js';

// Populated by PR 5 (feat/scheduled-events).
export function makeScheduledEventStubs(): ScheduledEventCapability {
    return {};
}
